function email()
{
    with(emal)
    {
        apos=value.indexOf(@);
        dotpos=value.lastIndexOf(".");
        if(apos<1||dotpos-apos<2)
        {
            
            span1.innerText="请输入正确格式";
        }
}